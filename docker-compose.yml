services:
  tcp-server:
    image: python:3.11-slim
    volumes:
      - .:/app
    working_dir: /app
    command: python -u tcp-server.py
    ports:
      - "9999:9999"
    deploy:
      resources:
        limits:
          cpus: '1.0'
          memory: 512M
    restart: unless-stopped
